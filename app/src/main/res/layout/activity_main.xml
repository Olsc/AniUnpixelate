<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:fitsSystemWindows="false">

    <!-- Top Toolbar (Clean White Style) -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        app:elevation="1dp">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:title="@string/app_title"
            app:titleTextColor="@color/text_primary"
            app:titleCentered="true">
            
            <ImageButton
                android:id="@+id/btn_toolbar_pick"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_gravity="end"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_menu_gallery"
                app:tint="@color/text_primary"
                android:layout_marginEnd="8dp"
                android:visibility="gone"/>
        </com.google.android.material.appbar.MaterialToolbar>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- CONTENT FRAME -->
    <FrameLayout
        android:id="@+id/content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- STEP 1: IMPORT (Simple Style) -->
        <LinearLayout
            android:id="@+id/step_1_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:visibility="visible">
            
            <ImageView
                android:layout_width="120dp"
                android:layout_height="120dp"
                android:src="@mipmap/ic_app_logo"
                android:layout_marginBottom="32dp"
                android:scaleType="centerInside" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/app_title"
                android:textColor="@color/text_primary"
                android:textSize="28sp"
                android:fontFamily="sans-serif-medium"
                android:layout_marginBottom="8dp"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/app_subtitle"
                android:textColor="@color/text_secondary"
                android:textSize="16sp"
                android:layout_marginBottom="48dp"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_main_pick"
                style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
                android:layout_width="240dp"
                android:layout_height="56dp"
                android:text="@string/btn_open_gallery"
                android:textAllCaps="false"
                android:textSize="16sp"
                app:cornerRadius="12dp"
                app:backgroundTint="@color/accent_blue"
                app:icon="@android:drawable/ic_menu_add" />
        </LinearLayout>

        <!-- STEP 2: SELECTION (Simplified UI) -->
        <FrameLayout
            android:id="@+id/step_2_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone">

            <com.olsc.removemosaic.MosaicView
                android:id="@+id/mosaic_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/white" />

            <!-- Instruction Banner -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="16dp"
                android:text="@string/instruction_selection"
                android:textColor="@color/text_primary"
                android:textSize="14sp"
                android:textAlignment="center"
                android:background="#E0FFFFFF" />

            <!-- Tool Bar (Simple Bottom Bar) -->
            <androidx.cardview.widget.CardView
                android:layout_width="wrap_content"
                android:layout_height="64dp"
                android:layout_gravity="bottom|center_horizontal"
                android:layout_marginBottom="32dp"
                app:cardCornerRadius="32dp"
                app:cardElevation="4dp"
                app:cardBackgroundColor="@color/white">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:orientation="horizontal"
                    android:paddingHorizontal="16dp"
                    android:gravity="center">

                    <ImageButton
                        android:id="@+id/btn_undo"
                        android:layout_width="48dp"
                        android:layout_height="48dp"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:src="@android:drawable/ic_menu_revert"
                        app:tint="@color/text_primary" />

                    <ImageButton
                        android:id="@+id/btn_clear"
                        android:layout_width="48dp"
                        android:layout_height="48dp"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:src="@android:drawable/ic_menu_delete"
                        app:tint="@color/text_primary"
                        android:layout_marginHorizontal="4dp"/>

                    <View android:layout_width="1dp" android:layout_height="24dp" android:background="#E0E0E0" android:layout_marginHorizontal="12dp"/>

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btn_to_step3"
                        style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
                        android:layout_width="wrap_content"
                        android:layout_height="40dp"
                        android:text="@string/btn_continue"
                        android:textAllCaps="false"
                        android:textSize="14sp"
                        app:cornerRadius="20dp"
                        app:backgroundTint="@color/accent_blue" />
                </LinearLayout>
            </androidx.cardview.widget.CardView>
        </FrameLayout>

        <!-- STEP 3: RESULT AND PROCESSING -->
        <FrameLayout
            android:id="@+id/step_3_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone">

            <com.github.chrisbanes.photoview.PhotoView
                android:id="@+id/img_result"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="fitCenter" />

            <!-- Simple Loading Overlay -->
            <FrameLayout
                android:id="@+id/loading_overlay"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#A0FFFFFF"
                android:visibility="gone"
                android:clickable="true">
                
                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:orientation="vertical"
                    android:gravity="center"
                    android:padding="32dp">

                    <ProgressBar
                        android:id="@+id/progress_bar"
                        android:layout_width="48dp"
                        android:layout_height="48dp"
                        android:indeterminateTint="@color/accent_blue"
                        android:layout_marginBottom="16dp" />

                    <TextView
                        android:id="@+id/txt_loading_status"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/status_fluid_logic"
                        android:textColor="@color/text_primary"
                        android:textSize="16sp" />
                </LinearLayout>
            </FrameLayout>

            <!-- Result Control Bar -->
            <androidx.cardview.widget.CardView
                android:id="@+id/result_controls"
                android:layout_width="wrap_content"
                android:layout_height="64dp"
                android:layout_gravity="bottom|center_horizontal"
                android:layout_marginBottom="32dp"
                android:visibility="visible"
                app:cardCornerRadius="32dp"
                app:cardElevation="4dp"
                app:cardBackgroundColor="@color/white">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:orientation="horizontal"
                    android:paddingHorizontal="16dp"
                    android:gravity="center">

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btn_back_to_selection"
                        style="@style/Widget.MaterialComponents.Button.TextButton"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/btn_refine_area"
                        android:textColor="@color/text_primary"
                        android:textAllCaps="false"
                        android:textSize="14sp" />

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/btn_process_action"
                        style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
                        android:layout_width="wrap_content"
                        android:layout_height="40dp"
                        android:text="@string/btn_action_unpixelate"
                        android:textAllCaps="false"
                        android:textSize="14sp"
                        app:cornerRadius="20dp"
                        app:backgroundTint="@color/accent_blue"
                        android:layout_marginHorizontal="8dp" />

                    <ImageButton
                        android:id="@+id/btn_save"
                        android:layout_width="48dp"
                        android:layout_height="48dp"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:src="@android:drawable/ic_menu_save"
                        app:tint="@color/text_primary" />
                </LinearLayout>
            </androidx.cardview.widget.CardView>
        </FrameLayout>

    </FrameLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
